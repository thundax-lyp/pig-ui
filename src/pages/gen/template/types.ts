export type TemplateItem = {
    id: string;
    templateName: string;
    generatorPath: string;
    templateDesc: string;
    templateCode: string;
    createTime?: string;
};

export type TemplateFormState = {
    id: string;
    templateName: string;
    generatorPath: string;
    templateDesc: string;
    templateCode: string;
};
